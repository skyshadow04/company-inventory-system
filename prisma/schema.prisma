// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Get a free hosted Postgres database in seconds: `npx create-db`

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User{
  id Int @id @default(autoincrement())
  name String
  email String @unique
  password String 
  role String @default("staff")
  createdAt DateTime @default(now())
}

model Supplier{
  supplier_id Int @id @default(autoincrement())
  supplier_name String
  supplier_contact_number String
  isActive Boolean @default(true)
  created_at DateTime @default(now())
  items Items[]
}

model Items{
  item_id Int @id @default(autoincrement())
  item_name String
  item_serial_number String @default("")
  item_code String @default("")
  item_price String
  item_type String
  item_quantity Int @default(0)
  item_description String @default("")
  supplier_id Int
  item_file_link String @default("https://u1ziwjw0vor3e4ce.public.blob.vercel-storage.com/Signed%20Quotation/Logo%20Image.jpg")
  item_file_photo_link String @default("https://u1ziwjw0vor3e4ce.public.blob.vercel-storage.com/Signed%20Quotation/Logo%20Image.jpg")
  item_delivery_date DateTime @default(now())
  supplier Supplier @relation(fields: [supplier_id], references: [supplier_id])
}

model Assets{
  asset_id Int @id @default(autoincrement())
  asset_name String
  asset_serial_number String @default("")
  asset_owner String
  asset_status String @default("Used")
  asset_type String 
  asset_image_link String @default("https://u1ziwjw0vor3e4ce.public.blob.vercel-storage.com/Signed%20Quotation/Logo%20Image.jpg")
}